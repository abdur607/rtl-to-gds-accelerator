# Surviving DRC/LVS result

The surviving project record states that DRC and LVS violations were identified and resolved before the final GDSII result. The original rule names, intermediate violation counts, final sign-off summary, and layout database were lost. No zero-violation report is fabricated here.

A current sign-off artifact should include tool/version, rule deck identifier that can legally be named, final violation/error counts, and a concise explanation of waivers if any.
